# CivicGH (Expo)

Expo Go loads the real CivicGH website from this repo (map, Road Assist, the same screens as the browser).

```bash
# Terminal 1, from the repo root
npm run dev

# Terminal 2, from this folder
npm install
npx expo start
```

Scan the QR with Expo Go. See [EXPO.md](../EXPO.md).
